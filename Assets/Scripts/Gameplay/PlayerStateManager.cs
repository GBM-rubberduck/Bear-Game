using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerStateManager : MonoBehaviour
{
    /*HealthManager playerHealthManager;

    public enum PLAYER_STATES {idle, running, jumping };
    public int currentPlayerState = 0;
    
    /*------- Week 8: ------  ------  Challenge 5 ------  ------  Harder! ------
     * 1. Define a new public variable to store player's currect state (idle, running, jumping ,...) (I have done this already below this comment)
     * 2. Update player state variable from Player controller class 
     * 3. Use the player state from this script inside your PlayerAnimationController class to update animations accordingly.
     */
    // Start is called before the first frame update

    /*void Start()
    {
        playerHealthManager = GetComponent<HealthManager>();
        print(playerHealthManager.GetHealth());

        
    }

    // Update is called once per frame
    void Update()
    {
        if(Input.GetKeyUp(KeyCode.G))
        {
            playerHealthManager.TakeDamage(2f); 

        }
        if (Input.GetKeyUp(KeyCode.H))
        {
            playerHealthManager.Heal(1f);

        }
    }*/
}
